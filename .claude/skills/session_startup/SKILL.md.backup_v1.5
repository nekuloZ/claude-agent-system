---
name: 会话启动
description: 智能识别会话启动意图，支持直接问候、模糊表达、首次对话自动触发。读取L0+L1+L4，分析上次对话，基于时间段和作息习惯推荐任务，主动发现重复模式。
version: v1.5
last_updated: 2026-01-24
changelog:
  - v1.5 (2026-01-24): 新增洞察能力 - 读取 insight_index.json，识别并提示高频主题
  - v1.4 (2026-01-19): 适配 habit_data.json 历史队列结构 - 写入队列而非覆盖，检查昨天数据是否归档
  - v1.3 (2026-01-18): 移除能量时段概念，改为基于时间段+作息习惯（洗澡日/不洗澡日）的查表逻辑
  - v1.2 (2026-01-17): 修复上下文工程问题 - 添加habit_data.json、从user_profile读取routine规则、明确洗澡日判断流程
  - v1.1 (2026-01-10): 与CLAUDE.md同步优化
  - v1.0 (2026-01-06): 初始版本
---

# 会话启动 Skill

智能识别用户启动会话的意图，提供个性化的问候和任务推荐。

## 智能触发机制

### 1. 直接触发（高置信度）

**明确问候：**
- "早上好" / "中午好" / "晚上好"
- "hey jarvis" / "你好" / "hi" / "嗨"

**归档指令：**
- "Archive" / "复盘" / "总结今天"

### 2. 模糊触发（需要智能判断）

**模糊问候：**
- "早" / "哈喽" / "在吗"
- 表达式："今天.*怎么样|今天.*计划|现在.*做什么"

**上下文判断：**
- 时间：早上06:00-12:00 → 倾向于启动问候
- 时间：晚上19:00-22:00 → 可能是归档意图
- 首次对话：今日第一次对话 → 自动触发问候

### 3. 自动触发（无需特定表达）

**条件：**
- 今日首次对话 AND 时间在 06:00-12:00
- 或：检测到时间/日期相关表达（"今天"、"现在"）

**处理：**
- 直接启动 Session Startup
- 如果用户内容完全不相关，先回复内容再询问

### 4. 语义触发（基于内容理解）

**习惯相关：**
- "今天感觉很累" → 询问睡眠情况
- "今天精神不错" → 询问睡眠和起床
- "昨天加班很晚" → 询问睡眠受影响

**工作相关：**
- "今天想开始工作" → 启动问候 + 推荐任务
- "现在该做什么" → 启动问候 + 查看待办

## 意图识别优先级

当收到用户消息时，按以下顺序判断：

1. **明确指令** > 模糊表达 > 自动触发 > 语义触发
2. 如果置信度 < 0.6，先确认意图
3. 如果多意图冲突，选择优先级最高的

## 执行步骤

### ⚠️ 前置步骤（必须首先执行）

**获取当前时间 - 禁止跳过，禁止猜测，禁止与后续步骤并行**

必须使用系统命令获取当前时间。任何涉及时间的判断都必须基于真实的时间数据。

```bash
powershell -Command "Get-Date -Format 'HH:mm'"      # 获取当前时间
powershell -Command "Get-Date -Format 'yyyy-MM-dd'"  # 获取当前日期
powershell -Command "Get-Date -Format 'dddd'"        # 获取星期几
```

**执行约束：**
- 必须在读取任何文件之前执行
- 必须在输出任何问候语之前执行
- 禁止与读取文件并行执行
- 禁止从文件内容"推断"或"假设"当前时间

---

### 1. 读取必需文件

```
✅ L0: jarvis-memory/L0_Working/.last_session.json (今日会话摘要)
✅ L0: jarvis-memory/L0_Working/next_actions.md (当前待办)
✅ L0: jarvis-memory/L0_Working/habit_data.json (习惯数据，包含last_shower_date等)
✅ L4: jarvis-memory/L4_Identity/user_profile.md (核心身份+Daily Routine)
✅ L1: jarvis-memory/L1_Episodic/projects_index.json (项目索引)
✅ L1: jarvis-memory/L1_Episodic/insight_index.json (洞察索引，重复模式)
```

### 1.5. 分析高频主题（新增）

**⚠️ 新功能（v1.5）：主动发现重复模式**

**从 insight_index.json 提取高频主题：**

**筛选条件：**
- count ≥ 3（出现至少 3 次）
- last_seen 在最近 7 天内
- 按 count 排序，取前 3 个

**提取维度：**
- recurring_problems - 反复出现的问题
- behavior_patterns - 有规律的行为
- emotional_patterns - 情绪/能量规律
- recurring_concerns - 反复的担心

**输出格式（内部使用，不直接展示给用户）：**
```json
{
  "high_frequency_themes": [
    {
      "type": "recurring_problems",
      "name": "环境配置问题",
      "count": 5,
      "last_seen": "2026-01-22",
      "contexts": ["AI电商图", "Jarvis系统"]
    }
  ]
}
```

**使用策略：**
- 如果有 1 个高频主题 → 在问候语后自然提及
- 如果有 2+ 个 → 只提最频繁的 1 个
- 提及方式：观察式提问，而非陈述

**示例：**
```
用户："早上好"

系统：(读取 insight_index.json)
      → 发现"项目管理混乱" count=4, last_seen=2026-01-22

系统："早上好！现在是09:30。
      注意到你最近几次都提到项目管理的事，今天要处理吗？

      昨晚几点睡的？今早几点起的？"
```

**注意事项：**
- 保持自然，不要生硬
- 不确定时用"好像..."、"注意到..."等缓和语气
- 用户可以直接回答或跳过
- 每次会话最多提 1 个观察

### 2. 任务推荐逻辑

**核心原则：从 user_profile.md 读取作息规则，按时间段查表推荐，不用抽象概念**

**Step 1: 判断今天类型**

从 `user_profile.md` 的"今天类型判断"读取规则，从 `habit_data.json` 读取 `last_shower_date`，计算今天是洗澡日还是不洗澡日。

**规则：**
- (当前日期 - last_shower_date) % 2 == 0 → 洗澡日
- (当前日期 - last_shower_date) % 2 == 1 → 不洗澡日

**含义：** last_shower_date 是洗了澡的那天，偶数天后再次洗澡。

**Step 2: 判断是否工作日**

从 `user_profile.md` 读取 Work Schedule（如"周一到周六，09:00-18:30"），结合当前星期几，判断今天是否工作日。

**Step 3: 读取对应 routine**

根据今天类型，从 `user_profile.md` 读取：
- **洗澡日** → 读取"洗澡日 routine"
- **不洗澡日** → 读取"不洗澡日 routine"

**Step 4: 根据当前时间推荐活动**

对照 routine 中的时间段，给出当前应该做什么的建议。

例如（具体内容以 user_profile.md 为准）：
- 06:00-08:59 → 鸣潮（支线/每日）
- 09:00-18:30（工作日）→ 工作时间，推荐 next_tasks
- 周末/非工作日 → 不推荐工作任务，按 routine 或询问用户安排

**Step 5: 工作时间推荐任务**

如果当前是工作日的工作时间（09:00-18:30）：
- 从 next_actions.md 读取"今日主要任务"
- 优先推荐今日任务
- 结合项目进度给出建议

**关键原则：**
- **数据与逻辑分离：** routine 规则全部在 user_profile.md，skill 只负责读取和执行
- **基于时间段：** 不用抽象概念，直接对照 routine 的时间段
- **判断工作日：** 根据 Work Schedule 判断是否推荐工作任务

### 3. 分析上次对话

从 .last_session.json 读取：
- 最后会话时间
- 最后讨论主题
- 今日所有活动记录
- 当前焦点

**格式：** "上次对话：[HH:MM] [主题]"

**如果 .last_session.json 不存在或为空：**
- 跳过"上次对话"显示
- 直接进入问候和状态询问

**示例：**
- "上次对话：09:22 讨论远程访问方案"
- "上次对话：14:30 AI电商图项目前端优化"

### 4. 输出问候语

```
[早上好/中午好/晚上好]！现在是 [HH:MM]。

[如果有高频主题（count ≥ 3 且 last_seen 在 7 天内）→ 自然融入观察]
示例：
- "注意到你最近几次都在说XX的事，今天要处理吗？"
- "好像最近经常遇到XX问题，要不要今天彻底解决它？"
- "你上周也提到XX的担心，想聊聊吗？"

[根据时间段给出活动建议，例如：
- 06:00-08:59 → 现在是鸣潮时间
- 09:00-18:30 → 接近工作时间/工作时间，今日任务：[从next_actions读取]
- 其他时间段 → 按照routine建议]

上次对话：[时间] [主题]（如果 .last_session.json 为空则跳过）

[根据活动内容和项目状态继续对话]
```

**高频主题融入原则：**
- 放在问候语之后，任务推荐之前
- 用观察式提问，不用陈述
- 只提 1 个，避免信息过载
- 用户可以选择回应或跳过

### 5. 询问生活状态（简化版）

**读取 L4 目标：**
```
jarvis-memory/L4_Identity/user_profile.md
```

**只问关键问题：**
```
昨晚几点睡的？今早几点起的？

（如果用户回答了，继续）
中午/晚上吃的什么？

（可选，如果有特殊情况）
身体有什么不适吗？
```

**为什么只问这几个：**
- 睡眠和起床是最基础的习惯
- 饮食是次要的
- 身体不适只在有异常时记录

**不要连续问太多问题**，避免用户疲劳。

**数据处理：**
- 提取：actual 值（如 "22:30"、"07:00"）
- 对比：从 L4 读取 target 值
- 判断：achieved = actual 是否在 target ±30分钟内
- 记录：**写入 habit_data.json 的 history 队列**
  - 检查 history[0] 的日期是否是昨天
  - 如果是昨天且未归档，提示用户："⚠️ 检测到昨天的习惯数据尚未归档，建议先执行'Archive'复盘"
  - 将今天的记录追加到 history 数组开头
  - 保持最近 7 天的数据，删除超过 7 天的记录

### 6. 记录到 L3 课题文件（如适用）

如果发现某个维度持续存在问题，更新到对应的 L3 课题文件：
- 睡眠/运动问题 → `jarvis-memory/L3_Semantic/themes/健康管理.md`
- 情绪问题 → `jarvis-memory/L3_Semantic/themes/情绪管理.md`
- 工作效率 → `jarvis-memory/L3_Semantic/themes/效能管理.md`
- 其他维度 → 对应的 L3 课题文件或创建新文件

## 智能意图识别交互示例

### 示例1：首次对话自动触发

**场景：早上9点，用户今日第一次对话**

```
用户：今天天气不错

AI检测：
- 时间：09:00（早间时段）
- 首次对话：是
- 意图：模糊 → 自动触发 Session Startup

AI回复：
早上好！天气不错心情应该也很好吧。

昨晚几点睡的？今天几点起的？现在精力状态如何？
```

---

### 示例2：语义触发 - 习惯追踪

**场景：用户表达状态，AI 主动采集习惯数据**

```
用户：今天感觉很累

AI检测：
- 关键词："累"
- 意图：可能是睡眠问题（40%）或工作累（30%）

AI回复：
听起来很累。是因为没睡好，还是工作太累？

[用户回答后继续采集数据]
```

**场景2-1：用户确认睡眠问题**

```
用户：可能是没睡好

AI检测：
- 意图确认：睡眠问题

AI回复：
昨晚几点睡的？今早几点起的？

[采集习惯数据并分析]
```

**场景2-2：用户确认工作累**

```
用户：昨天加班到很晚

AI检测：
- 意图：工作影响睡眠

AI回复：
那昨晚几点睡的？睡眠怎么样？

[采集习惯数据并分析]
```

---

### 示例3：模糊问候识别

**场景：各种模糊表达**

```
用户：早
AI：早上好！昨晚几点睡的？今早几点起的？

用户：哈喽
AI：早上好！昨晚睡得怎么样？

用户：在吗
AI：在的。早上好！昨晚几点睡的？

用户：今天
AI：早上好！昨晚睡得怎么样？今天计划做什么？
```

---

### 示例4：工作相关触发

**场景：用户想开始工作**

```
用户：今天想开始工作了

AI检测：
- 关键词："工作"
- 意图：启动工作 → 需要先了解状态

AI回复：
早上好！在开始工作前，先了解一下你的状态。

昨晚几点睡的？今早几点起的？现在精力如何？
```

---

### 示例5：多意图确认

**场景：意图不明确，主动确认**

```
用户：整理一下

AI检测：
- 可能意图1：归档复盘（40%）
- 可能意图2：整理文件（30%）
- 可能意图3：整理思路（20%）
- 置信度：< 0.6

AI回复：
你是想：
1. 复盘今天的记录
2. 整理某个文件
3. 其他？

[等待用户确认后执行]
```

---

## 注意事项

- ⚠️ **时间必须用命令获取，禁止猜测**
- 跨设备切换时要显示上次对话信息
- 如果L0为空或无今日记录，不显示"上次对话"
- 保持对话自然，避免过度结构化
- **智能触发的核心：让用户无需记住触发词，自然表达即可**
- **当意图模糊时，主动确认而不是盲目执行**
- **优先响应用户的直接需求，再引导到习惯追踪**
- **任务推荐基于：时间段 + 作息习惯 + 待办事项**
- **按时间段推荐，简单直接，不要过度分析**
